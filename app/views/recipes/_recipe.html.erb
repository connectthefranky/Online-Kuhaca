<!DOCTYPE html>
<html lang="en">

  <body>
    <!-- Page Content -"->
    <!div class="row">-"->
      <!% obj.each do |recipe| %>--">
          <div class="col-xs-offset-3 col-xs-6">
            <div class="well">
              <!%= link_to (image_tag recipe.image.url(),height: '100', width: '100', class: 'image img-circle'), recipe %> -"->
              <strong id = "recipe_title"><!%= recipe.title %></strong>
            </div>
          </div>
      <!% end %>
    </div> -->
    <div class="container">
      <div class="row">
        <% obj.each do |recipe| %>
          <div class="col-sm-6 col-md-4">
            <div class="thumbnail polje-recept">
              <%= link_to (image_tag recipe.image.url(),height: '200', width: '400', class: 'image img-circle'), recipe %>
              <div class="caption">
                <h3 class="polje-naslov"><%= recipe.title %></h3>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <!-- jQuery -->
    <script src="js/jquery.js"></script>
    <!-- Bootstrap Core JavaScript -->
    <script src="js/bootstrap.min.js"></script>
  </body>
</html>
